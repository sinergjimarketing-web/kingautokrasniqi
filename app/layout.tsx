import type { Metadata } from 'next';
import { Inter, Playfair_Display } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });
const playfair = Playfair_Display({ subsets: ['latin'], variable: '--font-playfair' });

export const metadata: Metadata = {
  title: 'King Auto Krasniqi | Premium Car Rental in Albania',
  description: 'Luxury SUVs and premium cars for airport pickup, business trips, holidays and special occasions. Available 24/7.'
};

export default function RootLayout({
  children
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${playfair.variable} bg-charcoal text-white`}>{children}</body>
    </html>
  );
}
